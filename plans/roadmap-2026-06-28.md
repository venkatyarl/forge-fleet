# ForgeFleet Roadmap — 2026-06-28 (operator pivot)

**Context:** The Telegram bot (OpenClaw, ungrounded) had been fabricating a fictional
roadmap for days. The operator's directive: *build the real thing* — mature the ff
agent-swarm into a system that genuinely (a) answers truthfully over Telegram, (b) builds
products using **all 15 computers + their sub-agent worktrees** via the existing Pillar-4
pipeline, and (c) uses the hybrid local→cloud LLM cascade that already exists.

**Honest framing:** Most of the "Hybrid LLM Architecture" the bot bragged about already
EXISTS in ff (`fleet_run` tiered cascade, `fleet_crew`, `offload` router, `session_runner`,
`planner`, `consensus`, Pillar-4 build pipeline). The job is to **mature + close the gaps**,
not rebuild from scratch.

## E1 — Grounded Telegram (kills the hallucination) — P0
ff owns the Telegram conversation, not OpenClaw. Inbound message → answer from REAL data
(`ff pm board`, `fleet_workers`, `ff pm stats`) or route to `fleet_run`/the swarm; log every
turn to `ff_interactions`.
- E1.1 Take the bot token off OpenClaw's telegram channel (disable `channels.telegram`).
- E1.2 Enable + own the `forgefleetd` telegram transport (single poller).
- E1.3 Grounded answerer: "what are you working on" → `ff pm board`; "what computers" →
  `fleet_workers`; general → `fleet_run`. NEVER answer fleet/PM questions from an ungrounded LLM.
- E1.4 Log all telegram turns to `ff_interactions` (channel='telegram').

## E2 — Swarm reliability so fleet builds succeed — P0 (prereq for E3)
Fleet builds currently FAIL on priya/logan/beyonce (small per-slot ctx, only some nodes
agent-capable, no capability-aware router). See `project_fleet_agent_swarm_broken`.
- E2.1 Agent-capable serving profile (per-slot ctx ≥ tool-prompt size) on ≥3 nodes.
- E2.2 Capability-aware router: only assign code work_items to agent-capable endpoints.
- E2.3 Pillar-4 build smoke test: a trivial work_item must go ready→PR on a NON-Taylor node.

## E3 — Drive the roadmap THROUGH the fleet — P1
- E3.1 Re-point the autonomous loop: create work_items, mark `ready`, let Pillar-4 farm them
  to the 51 free slots — instead of shipping PRs solo on Taylor.
- E3.2 Keep the bug-hunting stream, but route those fixes through Pillar-4 too.

## E4 — Triage the 784 cortex-audit findings — P1
- E4.1 Classify `[cortex audit] endpoints-no-guard` rows: genuinely-unguarded vs intentionally-public.
- E4.2 Promote real security gaps to `ready` work_items; close the rest.
- E4.3 Fix the audit emitter to rank/dedupe/gate instead of flooding `idea`.

## E5 — Product surface the bot fantasized about (real versions) — P2
Build only after E1–E3 are solid. Re-scope each against what already exists before building.
- Self-healing/maintenance (much exists: revive, fleet_integrity, reapers — mature + verify).
- Web dashboard + monitoring (ff-mc dashboard exists — extend).
- Improved/agent-native TUI.
- Multi-modal ingest (video/image) — design first; honestly NOT built today.

## Done 2026-06-28
- MCP popup root cause: `ff mcp install --for claude-desktop` wrote `type:http` (Desktop
  rejects); now writes `npx mcp-remote` stdio bridge — **PR #641**, deployed.

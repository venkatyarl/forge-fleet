//! Library target for shared ForgeFleet CLI functionality.
//!
//! The CLI currently lives in the `forgefleet` binary; this target exists so
//! workspace library verification can compile and test the package uniformly.

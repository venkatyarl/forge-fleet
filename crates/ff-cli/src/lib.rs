//! Library target for the ForgeFleet CLI package.
//!
//! The executable implementation remains in `main.rs`; this target exists so
//! package-level library verification can run consistently across the workspace.

//! Library target for the ForgeFleet CLI package.
//!
//! The command-line application remains in `main.rs`; this target exists so
//! workspace verification can compile and test `ff-cli` with `cargo test --lib`.

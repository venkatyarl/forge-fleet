# Model scout denylist.
#
# HuggingFace model ids (case-insensitive) that the scout loop should
# NEVER auto-add to the catalog. Useful for:
#   - known-broken repos
#   - license-ambiguous repos that still pass the license filter
#   - anything the operator explicitly rejected via `ff model reject`
#
# One id per line in the `deny` array.

deny = [
    # Examples — uncomment / replace as needed.
    # "bigscience/bloom-176b",
    # "meta-llama/Llama-2-70b-hf",
]

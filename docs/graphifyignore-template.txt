# .graphifyignore — ForgeFleet Yarli_KnowledgeBase
# Graphify processes ALMOST everything. This file is intentionally minimal.
# Only exclude: graphify's own output, git internals, build artifacts, large binaries.

# ─── Graphify's own output (avoid recursive self-processing) ───
graphify/
graphify-codebase/
*/graphify-codebase/

# ─── Git internals ───
.git/

# ─── Build artifacts & caches ───
node_modules/
target/
dist/
build/
__pycache__/
*.egg-info/
.cache/

# ─── Large binary model files ───
*.bin
*.pkl
*.pt
*.safetensors
*.gguf
*.onnx

# ─── Temporary files ───
*.tmp
*.swp
*.swo
*~

# ─── Obsidian system ───
.obsidian/
.trash/

# ─── ForgeFleet local cache (outside vault anyway) ───
# .forgefleet/ is not in the vault, no need to exclude
